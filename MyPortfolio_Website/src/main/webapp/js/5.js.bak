  
  let isloggedin=1;
 /* let option = isloggedin == 1 ? "logout":"login";
      document.write(option);  */

  if(isloggedin == 1){ document.write('logout');}
  else{ document.write( 'login');}